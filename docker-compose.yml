version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pde_dev
    ports:
      - "5432:5432"
    volumes:
      - pde_pgdata:/var/lib/postgresql/data
volumes:
  pde_pgdata:
